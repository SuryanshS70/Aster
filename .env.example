# Public browser configuration (safe to expose; currently unused)
VITE_API_BASE_URL=

# Application server
NODE_ENV=development
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info
REQUEST_BODY_LIMIT_BYTES=1048576
SHUTDOWN_TIMEOUT_MS=10000

# PostgreSQL (replace placeholders for local use)
POSTGRES_USER=aster
POSTGRES_PASSWORD=replace-with-a-local-password
POSTGRES_DB=aster
DATABASE_URL=postgresql://aster:replace-with-a-local-password@localhost:5432/aster

# Redis
REDIS_URL=redis://localhost:6379
REDIS_KEY_PREFIX=aster

# Reserved server-only secrets for later phases. Leave blank for Phase 2.
SESSION_SECRET=
GEMINI_API_KEY=
GEMINI_MODEL=
